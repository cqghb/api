server:
  port: 8081
  servlet:
    context-path: /server
#spring:
#  application:
#    name: api
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/api?serverTimezone=GMT%2B8
    username: root
    password: helloser
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  mapper-locations: classpath:mapper/*.xml # xml映射文件位置
  type-aliases-package: com.test.api.api.bean # 对应实体类的路径